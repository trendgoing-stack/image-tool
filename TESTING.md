# 実機確認チェックリスト

iPhone（iOS 17以降）の Safari で確認します。Service Worker は HTTPS でないと動かないため、
GitHub Pages（https://trendgoing-stack.github.io/image-tool/ ）に push してから確認してください。

確認前に、画面のいちばん下の**バージョン番号**が push したものと同じか確認します。
違う場合は、画面上部の「更新があります」をタップするか、しばらく待ってから開き直してください。

記録欄：日付・iOSバージョン・端末・結果（○ / ×）とメモ

## 1. 読み込みと向き

- [ ] **縦位置の写真**を「リサイズなし」で処理・保存すると、写真アプリで正しい向き（縦長）になっている
- [ ] 縦位置の写真を「長辺1080」で処理すると、高さが1080pxの縦長になる（結果欄が「3024×4032 → 810×1080」のように縦長の順になる）
- [ ] 横位置・逆さま（ホームボタン側を上にして撮影）の写真も正しい向きになる
- [ ] 前後比較の画面でも、処理前と処理後の向きがそろっている
- [ ] ファイルアプリから選んだ **HEIC** が読み込めない場合、その画像だけがエラー表示になり、ほかの画像は処理が続く

## 2. 大きな写真の自動縮小

- [ ] **24MP**（5712×4284）の写真を「リサイズなし」で処理すると、約4729×3547に縮小され、「自動で縮小しました」が表示される
- [ ] **48MP**（ProRAW以外の48MP撮影、8064×6048）の写真でも同様に縮小・通知され、クラッシュしない
- [ ] 48MPの写真を「長辺1920」で処理すると、自動縮小の通知は出ずに1920×1440になる
- [ ] 縮小後の画像がぼやけすぎていない（細かい文字などを前後比較で確認）

## 3. 圧縮と形式

- [ ] 画質スライダーを下げるほどファイルサイズが小さくなる
- [ ] 処理後のほうが大きくなった場合（小さなPNGをPNGで出力、など）に警告が出る
- [ ] **目標ファイルサイズ**（例：300KB）を指定すると、結果がそのサイズ以下になっている
- [ ] 目標を極端に小さく（例：5KB）すると「最低画質でも目標サイズに届かなかった」と表示される
- [ ] 出力形式を PNG にすると、画質・目標サイズ・背景色がグレーアウトし、理由が表示される
- [ ] 「元のまま」で、PNGはPNG、JPEG・HEIC（写真アプリから選択）はJPEGで出力される

## 4. 透過PNG → JPEG の背景色

- [ ] 透過のあるPNG（スクリーンショットを切り抜いたもの、ロゴ画像など）を JPEG＋「白」で処理すると、透過部分が白になる
- [ ] 「黒」で黒に、「任意」で選んだ色になる
- [ ] 透過のないJPEGは、背景色を変えても見た目が変わらない

## 5. 一括処理

- [ ] **10枚以上**（できれば24MPの写真を20枚ほど）を「リサイズなし」で一括処理しても、クラッシュ・再読み込みしない
- [ ] 同じ枚数を「長辺1920」でも処理できる
- [ ] 処理中は「処理中… n / 全体 枚」と進捗バーが進み、画面が固まらない（スクロールできる）
- [ ] 「中止」を押すと、処理中の1枚が終わったところで止まる
- [ ] サムネイルをタップすると前後比較が開き、境目を指でなぞって動かせる（ページがスクロールしない）

## 6. 保存

- [ ] 1枚の「保存」で共有シートが開き、「画像を保存」で写真アプリに入る
- [ ] ファイル名が `元の名前_1920px.jpg`（リサイズなしは `_compressed`）になっている（「"ファイル"に保存」で確認）
- [ ] 保存した写真の「情報」（ⓘ）に**位置情報が表示されない**
- [ ] 2枚以上で「**まとめて共有**」→「画像を保存」で、全部の写真が写真アプリに入る
- [ ] 「**ZIPで保存**」→「"ファイル"に保存」でZIPを保存でき、ファイルアプリでタップすると展開できる。日本語のファイル名が文字化けしない
- [ ] 「ZIPの準備ができました（タップで保存）」と表示された場合は、もう一度タップすると保存できる
- [ ] 共有シートを「×」で閉じても、エラーにならない

## 7. プリセット

- [ ] 名前を付けて保存したプリセットをタップすると、その設定が戻る
- [ ] 同じ名前で保存すると上書きの確認が出る
- [ ] × で削除できる（確認が出る）
- [ ] アプリを閉じて開き直しても、前回の設定とプリセットが残っている

## 8. PWA（ホーム画面）

- [ ] 「ホーム画面に追加」でアイコンが表示され、名前が「画像変換」になる
- [ ] ホーム画面から起動すると、Safariのアドレスバーなしの全画面で開く
- [ ] ノッチ・ホームインジケーターに文字やボタンが隠れない（ヘッダー、画面下のボタン、前後比較、ヘルプ）
- [ ] ダークモードに切り替えると、画面が暗い配色になる
- [ ] **機内モード**にしてからホーム画面のアイコンで起動し、画像を選択・処理・保存できる
- [ ] Safariで保存したプリセットが、ホーム画面版には出てこない（別々になる仕様どおり）

## 9. Service Worker の更新

1. ホーム画面のアプリを一度起動して閉じる
2. PCでバージョンを上げて push する（`node scripts/bump-version.mjs 0.x.y` → commit → push）
3. 数分後、ホーム画面のアプリを起動する（または、開いたまま一度ほかのアプリに切り替えて戻る）

- [ ] 画面上部に「**更新があります（タップで再読み込み）**」が表示される
- [ ] タップすると再読み込みされ、画面下のバージョンが新しい番号になる
- [ ] 更新後も、機内モードで起動できる（新しいバージョンがキャッシュされている）

## 10. 1枚編集モード

画面上部の「1枚を編集」に切り替えて確認します。保存結果は写真アプリで拡大して、プレビューと見比べてください。

### 加工位置のずれ
- [ ] **縦位置の写真**で、顔や文字をぼかした位置が、保存結果でも同じ場所になっている（上下左右や回転のずれがない）
- [ ] **24MP の写真**（自動縮小される大きさ）で、四角・ブラシの加工位置が保存結果とずれていない
- [ ] 48MP の写真でも加工して保存でき、クラッシュしない
- [ ] 画像の端（上下左右ぎりぎり）を囲んでも、端まで加工されている（元の画像が透けて見えない）
- [ ] 「長辺1080」などリサイズと組み合わせても、加工位置が合っている

### タッチ操作
- [ ] **ブラシでなぞっている間、ページがスクロールしない**
- [ ] 画像の上で 2本指でつまんでも、**ページがズームしない**
- [ ] 画像の上でダブルタップしても、ページがズームしない
- [ ] 画像の外（設定欄など）では、これまでどおりスクロールできる
- [ ] 四角の範囲を指定するとき、指が画像の外に出ても枠が追従し、離すと加工される

### ぼかし・モザイク・塗りつぶし
- [ ] ぼかし・モザイクの**強さ 1（最低）**でも、写真の中の文字（看板・書類など）が読めない
- [ ] 強さを上げると、よりぼける・ブロックが大きくなる
- [ ] 塗りつぶしで選んだ色になる
- [ ] 「確実に隠したい情報には塗りつぶしをおすすめします」の注意が表示されている

### 文字入れ
- [ ] 「＋ 文字を追加」で文字が中央に出て、ドラッグで移動できる
- [ ] 2本指で文字を広げる・つまむと大きさが変わり、スライダーの値も連動する
- [ ] 改行した文字が複数行で表示される
- [ ] 文字色・縁取りの色と太さ・半透明の背景帯の ON/OFF が反映される
- [ ] フォント（ゴシック・明朝・等幅など）を変えると見た目が変わる
- [ ] 追加済みの文字をタップすると選択でき、内容を書き換えたり削除したりできる
- [ ] **文字の位置・大きさ・改行位置が、プレビューと保存結果で一致している**（縦位置写真・24MP の写真の両方）

### 元に戻す・やり直す
- [ ] 「戻す」で直前の加工が取り消され、「やり直す」で戻る
- [ ] 文字を 1文字ずつ入力したあと「戻す」を押すと、入力した文字がまとめて取り消される
- [ ] 「全リセット」ですべての加工が消え、「戻す」で元に戻せる
- [ ] 別の画像を選び直すと、加工がリセットされる

## 回転・反転・切り取り（1枚を編集 →「回転・切取」）

### 回転・反転
- [ ] 「右に回転」「左に回転」で90°ずつ回り、保存結果も同じ向きになる
- [ ] 「左右反転」「上下反転」が、画面で見た向きのとおりに反転する（回転したあとでも）
- [ ] **縦位置の写真**を回転しても、保存結果の向きがプレビューと同じ

### 切り取り
- [ ] 枠の四隅をドラッグして大きさを変えられる。「自由」のときは辺もドラッグできる
- [ ] 枠の中をドラッグすると枠ごと移動し、画像の外にははみ出さない
- [ ] 比率（1:1・4:3・3:4・16:9・9:16・元の比率）を選ぶと、その形の最大の枠になり、四隅をドラッグしても形が保たれる
- [ ] 比率ボタンに用途の目安（Instagram投稿、ストーリーズなど）が表示されている
- [ ] 16:9 を選んでから回転すると、9:16 に切り替わる
- [ ] 「切り取り後：○×○ px」が保存結果の大きさと一致する（リサイズなしのとき）
- [ ] **切り取り枠を操作している間、ページがスクロール・ズームしない**
- [ ] 「切り取りを解除」で全体に戻る
- [ ] 切り取った画像のファイル名が `元の名前_長辺px.jpg` になる

### 加工との組み合わせ
- [ ] 先にぼかし・文字を入れてから回転・切り取りしても、写真の同じ場所に残る
- [ ] 回転・切り取りしてからぼかし・文字を入れると、保存結果でも見たとおりの位置になる
- [ ] 回転しても、文字は画面上でまっすぐのまま
- [ ] 切り取りで範囲の外に出たぼかし・文字は、保存結果に写っていない

### 解像度
- [ ] **48MP の写真**の一部（半分程度以下）を切り取ってリサイズなしで保存すると、元の解像度のまま（例：4032×3024）になり、自動縮小の通知が出ない
- [ ] 48MP の写真を切り取らずに回転だけすると、これまでどおり自動縮小され、通知が出る

### 戻す・やり直す
- [ ] 回転・反転・切り取りも「戻す」「やり直す」で1操作ずつ戻る
- [ ] 「全リセット」で回転・切り取りも含めて元に戻り、「戻す」で取り消せる

## 選択の解除

- [ ] まとめて処理：画像を選ぶと「選択を解除」が出て、押すと画像・処理結果が消え、「まだ選択されていません」に戻る
- [ ] 1枚を編集：「選択を解除」で画像・加工・処理結果が消え、編集画面が閉じる
- [ ] 処理結果や加工があるときは確認が出て、「キャンセル」なら何も消えない
- [ ] 解除しても、設定とプリセットは残っている
- [ ] 解除したあと、同じ画像を選び直せる
- [ ] 処理中は「選択を解除」が押せない

## 11. PC ブラウザ（最低限）

- [ ] Chrome / Edge で画像を選んで処理でき、「保存」でダウンロードされる
- [ ] 2枚以上で「ZIPで保存」を押すとZIPがダウンロードされる（共有に対応していない環境では「まとめて共有」は表示されない）
- [ ] 1枚編集モードで、マウスのドラッグで範囲指定・ブラシ・文字の移動ができる
